#include <iostream>
using namespace std;

int main() {
  //   vector<int> temp(nums.size());
  //
  //   for (int i = 0; i < nums.size(); i++) {
  //     temp[(i + k) % nums.size()] = nums[i];
  //   }
  //
  //   nums = temp;
}